query = select a.id,a.title, a.start_time start, a.end_time end,a.all_day allDay,a.color from agenda a where 1=1
        <#if end??>
            and end_time <= '${end}'
        </#if>
        <#if begin??>
            and start_time>= '${begin}'
        </#if>;