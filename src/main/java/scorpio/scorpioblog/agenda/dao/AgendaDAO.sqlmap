query = select a.id,a.title, a.startTime start, a.endTime end,a.allDay,a.color from agenda a where 1=1
        <#if end??>
            and startTime <= '${end}'
        </#if>
        <#if begin??>
            and endTime>= '${begin}'
        </#if>;